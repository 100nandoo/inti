# CLAUDE.md

See `AGENTS.md` for the shared project instructions.

## Agent skills

### Issue tracker

Issues for this repo live in GitHub Issues and should be managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

This repo uses the default triage label vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo: read the root `CONTEXT.md` first, then any relevant `docs/adr/` entries if they exist. See `docs/agents/domain.md`.
