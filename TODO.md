# Web

- synthesize button, speak and dowmload disable while and before synthesizing

# Server

- add dockerfile and docker compose
- add ffmpeg to support output as opus/mp3 or maybe try this instead of ffmpeg: https://github.com/kota-yata/byrd-mp3
