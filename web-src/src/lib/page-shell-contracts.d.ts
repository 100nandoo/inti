export type PageShellBadge = string;

export interface PageShellNavLink {
  href: string;
  label: string;
  title: string;
  iconClass: string;
}

export interface PageShellNavItem {
  path: string;
  label: string;
  title: string;
  iconClass: string;
}
